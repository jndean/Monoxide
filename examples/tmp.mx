
fn main()() {

    y := [0];
    x := &y[0];

    .i := 0;
    y[0] += 1;


    while(x < 3)
    {
        if(x==2) {
            x += 1;
        } else {
            x += 2;
        } ~if(x==2);
    }
    ~while(x > 0);

    $catch(1);$

} ~main()