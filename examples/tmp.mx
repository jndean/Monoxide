
fn main()() {
    x := [1,2,3];
    y <= x;
    y += 1;
    y => x;

    $x := 0;
    y := 1;
    z := &y;$
    $x, y => swap() => x, y;$
    $swap(x, z);$
} ~main()

fn swap<>(&A x, &A, y)() {
    
} ~swap()