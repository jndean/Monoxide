
fn main()() {

    x := 0;
    
    while(x < 3)
    {
        if(x==2) {
            x += 1;
        } else {
            x += 2;
        } ~if(x==2);
    }
    ~while(x > 0);

    catch(1);

} ~main()